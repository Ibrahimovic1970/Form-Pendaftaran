export const sendVerificationEmail = (email, name) => {
    console.log(`📧 Email dikirim ke: ${email}`);
};

export const sendWelcomeEmail = (email, name) => {
    console.log(`🎉 Selamat, ${name}! Anda telah berhasil diverifikasi.`);
};